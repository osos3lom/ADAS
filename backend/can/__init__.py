"""CAN bus interface (Phase-3 placeholder).

NOTE for Phase 3: this package is named ``can`` and will shadow the PyPI
``python-can`` library when the backend runs with its directory on sys.path.
When wiring real CAN in Phase 3, rename this package (e.g. ``canbus``) to avoid
the import clash with ``import can``.
"""
