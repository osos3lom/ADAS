"""CAN bus interface (Phase-3 placeholder).

Renamed from ``can`` to ``canbus`` so it no longer shadows the PyPI
``python-can`` library (``import can``) when the backend directory is on
sys.path. Real CAN wiring lands in Phase 3.
"""
