# ── Core web framework ────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
websockets>=13.0

# ── CAN bus ───────────────────────────────────────────────────────────────────
python-can>=4.4.0

# ── ADAS / perception (optional — uncomment for real sensor processing) ───────
# opencv-python>=4.10.0
# numpy>=1.26.0
# open3d>=0.18.0          # LiDAR point-cloud processing
# ultralytics>=8.0.0      # YOLOv8 object detection

# ── ROS2 P…(truncated)