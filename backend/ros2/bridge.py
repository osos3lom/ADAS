"""
ROS2 Bridge
-----------
Development stubs that simulate ROS2 node behaviour without requiring
a full ROS2 installation.  Replace these stubs with real rclpy nodes
when running on a machine with ROS2 Humble installed.

Production node map
───────────────────
Node              Language    Package               Description
perception        C++         adas_perception_pkg   LiDAR cluster + YOLOv8 camera detection
planning          Python      adas_planning_pkg     ADAS FSM (AEB / LDW / ACC stat…(truncated)