###############################################################################
#  ADAS-ECU Simulator — docker-compose.yml
#  Run on your hardware:  docker compose up --build
#
#  Services
#  ─────────
#    backend   Python 3.11 FastAPI — simulation engine + UDS server
#              http://localhost:8000   /docs   /health
#
#    frontend  Next.js 15 React dashboard
#              http://localhost:3000
#              BACKEND_URL is injected so all /api/sim/* calls proxy
#              transparen…(truncated)